# Add Pi worker badge to README

In the README.md of this repo, add a single new line near the top (after the title heading, before any existing badges) containing this exact markdown:

```
[![Built with Pi](https://img.shields.io/badge/built_with-pi-purple)](https://pi.dev)
```

Rules:
- ONLY modify README.md.
- Do not change any other file.
- If README.md does not exist, create it with a level-1 heading matching the repo name plus the badge line.
- Commit message: `chore: add Pi worker badge to README`.
- Keep the change minimal — no other edits, no reformatting.