# Visual QA Feedback Summary

> Generated by code analysis (OPENROUTER_API_KEY not available)

## Critical

### 1. Swagger UI dark mode broken (api page)
- **File:** `src/pages/Api.jsx`
- **Issue:** `.bg-white` class forces white background regardless of theme. `.swagger-ui` text is hardcoded to black.
- **Impact:** Dark mode renders white background on dark pages — jarring contrast mismatch.

### 2. ThemeToggle `w-4.5` invalid Tailwind value
- **File:** `src/components/ThemeToggle.jsx`
- **Issue:** `w-4.5 h-4.5` is non-standard Tailwind. Standard Tailwind uses increments of 1 (w-4, w-5).
- **Impact:** Icon may render at wrong size or silently fail in production builds.

## High

### 3. Hero background overflow on mobile (home)
- **File:** `src/components/home/Hero.jsx`
- **Issue:** `h-[120%] w-auto max-w-none` on the background SVG. On narrow viewports (390px), the large SVG can overflow horizontally.
- **Impact:** Horizontal scrollbar on mobile.

### 4. Code block background hardcoded (docs)
- **File:** `src/pages/Docs.jsx`
- **Issue:** `bg-[#0a0e17] border-[#1e293b]` hardcoded dark values in Code component. Does not adapt to light theme.
- **Impact:** In light mode, code blocks have very dark backgrounds that clash with the light theme.

### 5. Mobile playground accordion spacing
- **File:** `src/pages/Playground.jsx`
- **Issue:** Mobile accordion content uses `pt-1 pb-4` which provides minimal padding. Exercise search, layer editor and controls are crowded.

## Medium

### 6. Footer attribution link contrast
- **File:** `src/components/SiteFooter.jsx`
- **Issue:** `text-[11px]` is very small. On mobile viewports, the three items stack and can be hard to read.

### 7. Docs page TOC nav wrapping
- **File:** `src/pages/Docs.jsx`
- **Issue:** `flex-wrap gap-2` on the nav chips is fine, but there's no horizontal scroll container, causing items to wrap in an unbalanced way on in-between viewports.

### 8. Playground border contrast light theme
- **File:** `src/pages/Playground.jsx`
- **Issue:** Border color `#dfdfdf` in DEFAULT_SETTINGS vs light theme `--border: 220 13% 91%` — potential mismatch in border rendering.

## Low

### 9. Docs code font size
- **File:** `src/pages/Docs.jsx`
- **Issue:** Code component uses `text-[12px]` which is very small for readability, especially on mobile.

### 10. Pricing card spacing on small mobile
- **File:** `src/components/home/Pricing.jsx`
- **Issue:** `max-w-lg` on container may leave too much whitespace on very narrow viewports.